# Copy this file to .env and fill in your values
# Then source it before running nixos-rebuild:
#   source .env && sudo -E nixos-rebuild switch --flake .#nixos-amd

GIT_USER_NAME="chopshop1"
GIT_USER_EMAIL="mike@ezcorp.org"
