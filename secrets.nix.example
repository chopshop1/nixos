# Copy this file to secrets.nix and fill in your values
# secrets.nix is gitignored and will not be committed
#
# After creating secrets.nix, run:
#   git add -f secrets.nix
#
# This stages it for the flake to see, but it won't be committed due to .gitignore
{
  git = {
    userName = "Your Name";
    userEmail = "your.email@example.com";
  };
}
